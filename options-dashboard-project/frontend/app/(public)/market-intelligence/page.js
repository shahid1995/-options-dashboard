import MarketIntelligenceClientPage from "./ClientPage";

export const metadata = {
  title: "Market Intelligence — StrikeNova",
  description:
    "Go beyond the option chain. Analyze OI, volume, IV, Greeks, PCR, max pain and market structure across Indian index option chains.",
};

export default function MarketIntelligencePage() {
  return <MarketIntelligenceClientPage />;
}
