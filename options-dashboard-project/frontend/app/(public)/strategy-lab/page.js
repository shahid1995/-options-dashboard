import StrategyLabClientPage from "./ClientPage";

export const metadata = {
  title: "Options Strategy Builder & Analysis",
  description:
    "Build multi-leg option strategies, analyze payoff and risk, stress-test scenarios, and paper trade — all from the Strategy Lab.",
};

export default function StrategyLabPage() {
  return <StrategyLabClientPage />;
}
